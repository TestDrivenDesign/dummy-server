
const app = require('../app');
const request = require('supertest');

require('jest-sorted');
const {json} = require('express');

describe('POST /api/image', () => {
    test('server responds with 201 status and a number between 0 and 6', () => {
        
    })
})